name: claude-config
root: ~/.claude

# Project hooks for claude config management
on_project_start: |
  echo "🤖 Opening Claude config workspace..."
on_project_first_start: |
on_project_restart: |
on_project_exit: |

windows:
  - 🤖 claude-config:
      layout: main-horizontal
      panes:
        - nvim
  # - 📁 shell:
  #     layout: even-horizontal
  #     panes:
  #       - echo "Shell for Claude config management"
  #       - echo "💡 Use :VimuxRunCommand in nvim to run commands in split"
