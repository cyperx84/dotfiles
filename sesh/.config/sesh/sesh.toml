# Sesh Configuration - Tmuxinator-Only Mode
# All session layouts are managed through tmuxinator YAML files in ~/.config/tmuxinator/

blacklist = ["scratch"]

# Prioritize tmuxinator sessions first
sort_order = [
    "tmuxinator", # show first for complex layouts
    "config",
    "tmux",
    "zoxide", # show last
]

[default_session]
startup_command = "nvim"
preview_command = "eza --all --git --icons --color=always {}"
