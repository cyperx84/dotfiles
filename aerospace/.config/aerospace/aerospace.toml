after-startup-command = [
  'exec-and-forget sketchybar',
  'exec-and-forget borders'
]

exec-on-workspace-change = [
    '/bin/bash',
    '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE'
]

start-at-login = true
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true
accordion-padding = 1
default-root-container-layout = 'tiles'
# default-root-container-orientation = 'horizontal'
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']
automatically-unhide-macos-hidden-apps = true
[key-mapping]
    preset = 'qwerty'

[gaps]
    inner.horizontal = 20
    inner.vertical =   20
    outer.left =       20
    outer.bottom =     20
    outer.top =        52
    outer.right =      20

# ============================================================================
# Automatic Workspace Assignment
# These rules automatically move app windows to designated workspaces when
# they're opened, eliminating timing issues with manual move commands.
# No scripts or sleep delays needed!
# ============================================================================

# [[on-window-detected]]
# if.app-id = 'com.mitchellh.ghostty'
# run = 'move-node-to-workspace 1'
#
# [[on-window-detected]]
# if.app-id = 'app.zen-browser.zen'
# run = 'move-node-to-workspace 2'
#
# [[on-window-detected]]
# if.app-id = 'com.anthropic.claudefordesktop'
# run = 'move-node-to-workspace 3'
#
# [[on-window-detected]]
# if.app-id = 'com.google.antigravity'
# run = 'move-node-to-workspace 4'
#
# [[on-window-detected]]
# if.app-id = 'md.obsidian'
# run = 'move-node-to-workspace 6'

# ============================================================================
# System Apps - Auto-Float Rules
# These apps work better as floating windows (dialogs, utilities, etc.)
# ============================================================================

[[on-window-detected]]
if.app-id = 'com.apple.systempreferences'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.SystemProfiler'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.calculator'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.ActivityMonitor'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.finder'
if.window-title-regex-substring = 'Info|Copy|Move|Delete|Trash'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.notificationcenterui'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.SetupAssistant'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.ScreenSaver.Engine'
run = 'layout floating'

# App Store and Software Update
[[on-window-detected]]
if.app-id = 'com.apple.AppStore'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.SoftwareUpdate'
run = 'layout floating'

# Security & Privacy dialogs
[[on-window-detected]]
if.app-id = 'com.apple.SecurityAgent'
run = 'layout floating'

# Archive Utility (for .zip, .tar, etc.)
[[on-window-detected]]
if.app-id = 'com.apple.archiveutility'
run = 'layout floating'

[mode.main.binding]
    # App launchers with workspace switching
    # These launch the app AND switch to its workspace immediately
    # The on-window-detected rules above handle moving the window automatically
    ctrl-space = ['exec-and-forget open -na Ghostty']
    cmd-ctrl-t = ['exec-and-forget open -na Ghostty --args -e tmux attach']
    cmd-ctrl-shift-n = ['exec-and-forget open -na Ghostty --args -e tmux new-session -A -s Neovim nvim']
    cmd-ctrl-n = 'exec-and-forget open -na Ghostty --args -e bash -lc "/opt/homebrew/bin/sesh connect notes"'

    cmd-ctrl-b = ['exec-and-forget open -na Zen']
    cmd-ctrl-y = ['exec-and-forget open -a Zen "https://youtube.com"']
    cmd-ctrl-p = ['exec-and-forget open -a Zen "https://perplexity.com"']
    cmd-ctrl-g = ['exec-and-forget open -a "Google Chrome"']

    cmd-ctrl-c = ['exec-and-forget open -a Claude']
    cmd-ctrl-u = ['exec-and-forget open -a Antigravity']
    cmd-ctrl-a = ['exec-and-forget open -a "Ableton Live 12 Suite"']
    cmd-ctrl-d = ['exec-and-forget open -a "DaVinci Resolve"']
    cmd-ctrl-i = ['exec-and-forget open -a "iPhone Mirroring"']
    cmd-ctrl-m = ['exec-and-forget open -na Messages']
    cmd-ctrl-s = ['exec-and-forget ~/dotfiles/scripts/sidecar-ipad.sh']
    cmd-ctrl-o = ['exec-and-forget open -a Obsidian']

    # Window focus (shift + ctrl + hjkl)
    shift-ctrl-h = 'focus left'
    shift-ctrl-j = 'focus down'
    shift-ctrl-k = 'focus up'
    shift-ctrl-l = 'focus right'

    # Window movement (ctrl + alt + hjkl) - inverted for correct direction
    ctrl-alt-h = 'move right'
    ctrl-alt-j = 'move up'
    ctrl-alt-k = 'move down'
    ctrl-alt-l = 'move left'

    # Window movement with warp (shift + ctrl + alt + hjkl) - inverted for correct direction
    shift-ctrl-alt-h = 'move left'
    shift-ctrl-alt-j = 'move down'
    shift-ctrl-alt-k = 'move up'
    shift-ctrl-alt-l = 'move right'

    # Toggle window split
    shift-ctrl-e = 'layout tiling floating'

    # Layout toggles
    shift-ctrl-v = 'layout horizontal vertical'  # Toggle horizontal/vertical
    shift-ctrl-t = 'layout tiles'                # Tiling layout
    shift-ctrl-a = 'layout accordion'            # Accordion layout

    # Toggle float/unfloat
    shift-alt-t = 'layout floating tiling'

    # Float and center window
    shift-ctrl-q = 'layout floating tiling'

    # Maximize/fullscreen toggle
    shift-ctrl-w = 'fullscreen'

    # Resize windows (ctrl + alt + cmd + hjkl) - Smart resize adapts to window position
    ctrl-alt-cmd-j = 'resize smart -50'    # Make smaller (context-aware)
    ctrl-alt-cmd-k = 'resize smart +50'    # Make larger (context-aware)

    # Balance windows
    shift-ctrl-r = 'flatten-workspace-tree'

    # Workspace navigation (cmd + ctrl + jkl) HOMEROW Versions
    cmd-j = 'workspace 1'
    cmd-k = 'workspace 2'
    cmd-l = 'workspace 3'
    cmd-u = 'workspace 4'
    cmd-i = 'workspace 5'
    cmd-o = 'workspace 6'

    # Additional workspace navigation - binds need changing
    shift-ctrl-n = 'workspace next'
    shift-ctrl-p = 'workspace prev'

    # Focus and navigate to workspaces
    ctrl-shift-1 = 'workspace 1'
    ctrl-shift-2 = 'workspace 2'
    ctrl-shift-3 = 'workspace 3'
    ctrl-shift-4 = 'workspace 4'
    ctrl-shift-5 = 'workspace 5'
    ctrl-shift-6 = 'workspace 6'
    ctrl-shift-7 = 'workspace 7'
    ctrl-shift-8 = 'workspace 8'
    ctrl-shift-9 = 'workspace 9'

    # Move window to workspace
    shift-alt-j = 'move-node-to-workspace 1'
    shift-alt-k = 'move-node-to-workspace 2'
    shift-alt-l = 'move-node-to-workspace 3'
    shift-alt-u = 'move-node-to-workspace 4'
    shift-alt-i = 'move-node-to-workspace 5'
    shift-alt-o = 'move-node-to-workspace 6'

    shift-alt-1 = 'move-node-to-workspace 1'
    shift-alt-2 = 'move-node-to-workspace 2'
    shift-alt-3 = 'move-node-to-workspace 3'
    shift-alt-4 = 'move-node-to-workspace 4'
    shift-alt-5 = 'move-node-to-workspace 5'
    shift-alt-6 = 'move-node-to-workspace 6'
    shift-alt-7 = 'move-node-to-workspace 7'
    shift-alt-8 = 'move-node-to-workspace 8'
    shift-alt-9 = 'move-node-to-workspace 9'

    # Move window to previous/next workspace
    shift-alt-p = 'move-node-to-workspace --wrap-around prev'
    shift-alt-n = 'move-node-to-workspace --wrap-around next'

    # Move window between displays
    shift-alt-s = 'move-node-to-monitor --wrap-around prev'
    shift-alt-g = 'move-node-to-monitor --wrap-around next'

    # join-with
    cmd-alt-shift-h = 'join-with left'
    cmd-alt-shift-j = 'join-with down'
    cmd-alt-shift-k = 'join-with up'
    cmd-alt-shift-l = 'join-with right'

    # Service mode
    shift-alt-cmd-r = 'mode service'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = ['reload-config', 'mode main']
    r = ['flatten-workspace-tree', 'mode main'] # reset layout
    f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
    backspace = ['close-all-windows-but-current', 'mode main']

    # Pane operations in service mode
    shift-ctrl-alt-h = ['join-with left', 'mode main']
    shift-ctrl-alt-j = ['join-with down', 'mode main']
    shift-ctrl-alt-k = ['join-with up', 'mode main']
    shift-ctrl-alt-l = ['join-with right', 'mode main']
